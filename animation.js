(()=>{const items=document.querySelectorAll('.profile-copy > *, .portrait-figure, .section-heading > *, .org-card, .contact > *, footer > *');items.forEach((item,index)=>{item.classList.add('reveal');item.style.setProperty('--reveal-delay',((index%4)*75)+'ms')});if(window.matchMedia('(prefers-reduced-motion: reduce)').matches||!('IntersectionObserver' in window)){items.forEach(item=>item.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');observer.unobserve(entry.target)})},{threshold:.14,rootMargin:'0px 0px -7% 0px'});items.forEach(item=>observer.observe(item))})();
